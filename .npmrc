# 针对 Render（构建机在海外/美国）的 npm 稳健性配置。
# 注意：这里【刻意不】换成国内 npmmirror 镜像——Render 构建机在海外，
# 跨洋访问国内镜像通常反而更慢/更不稳；官方 registry 对海外实例本就是就近的。
# 这几项用于减少每次安装的额外耗时与偶发网络抖动，与 render.yaml 的缓存策略配合。
audit=false
fund=false
fetch-timeout=300000
fetch-retries=3
fetch-retry-maxtimeout=120000
loglevel=error
